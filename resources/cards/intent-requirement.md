# Requirement Intent Card

`requirement` 的目标是在既有架构内引入可控的行为增量。

Change Brief 记录：用户场景、当前行为、目标行为、In Scope / Out of Scope、兼容策略及 1～3 条可观察验收标准。

只有行为无歧义、复用现有模式、不新增业务状态且兼容策略明确时才允许 Micro Change。Focused Review 核对验收完整性、默认状态、旧行为兼容和权限边界；Targeted Verify 覆盖正常、空值/异常、禁用态与旧数据场景中的适用项。
